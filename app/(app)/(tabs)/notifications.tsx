import { LayoutProvider } from "@/components/layout";
import { ThemedText } from "@/components/themed-text";

const Notifications = () => {
  return (
    <LayoutProvider>
      <ThemedText>Notificaiton List</ThemedText>
    </LayoutProvider>
  );
};

export default Notifications;
